# 使用步骤（新手版）

本指南面向**非技术用户**，按步骤操作即可完成整组图片处理。

---

## 你需要准备的东西

请确认项目目录中有以下三个文件夹，并放入图片：

- `images/bg/`   背景图
- `images/pod/`  豆荚图
- `images/seed/` 种子图

三类图片中，**编号相同的文件是一组**。
例如：

```
images/bg/   GY2025HHN-0001.jpg
images/pod/  GY2025-0001.jpg
images/seed/ GY-0001.jpg
```

---

## 第一步：启动批处理

在命令行窗口中运行：

```
python batch_process.py
```

程序会提示：

> 从第几组开始？(1-xx，默认 1)

如果你是第一次做，直接回车即可。

---

## 第二步：清理背景图（每组都会出现）

1. 弹出背景窗口后，用鼠标拖拽画矩形，**框住需要删除的物体**（标尺、白牌等）。
2. 可以画多个矩形。
3. 按 **SPACE** 开始修复。
4. 修复完成后，窗口会再次弹出，如果还有不满意的地方可以继续框选。
5. 如果你不想再修了，**直接按 SPACE** 继续下一步。
6. 如果框选错了，**右键**可撤销上一个框选（或取消当前正在拖拽的框选）。
7. **R** 重做当前步骤，**S** 跳过当前步骤，**ESC** 退出当前窗口。

> 提示：本流程中所有“框选”窗口都支持右键撤销。

---

## 第三步：可选裁切

背景修复完成后会出现裁切窗口：

- 鼠标移动时会显示一条竖线
- **鼠标左键点击**：从竖线开始保留右边
- **按 SPACE**：跳过裁切

---

## 第四步：选择植株颜色参考（新增）

在放置豆荚/种子前，会弹出背景图让你框选一块“植株本体颜色”。  
这一步用于让豆荚/籽粒颜色更接近单株本体，而不是背景。  

操作：框选区域后按 **SPACE/ENTER** 确认。若不选，将使用默认参考色。

## 第五步：放置豆荚

1. 弹出豆荚图片窗口，拖拽框选豆荚区域。
2. 按 **SPACE/ENTER** 确认。
3. 进入放置窗口：
   - 鼠标移动调整位置
   - 滚轮缩放
   - 鼠标左键确认

---

## 第六步：放置种子

流程与豆荚相同：

1. 框选种子区域
2. 确认
3. 调整位置与大小
4. 左键确认

---

## 第七步：保存结果

程序会自动保存结果到：

```
outputs/final/<编号>_final.jpg
```

清理后的背景也会保存到：

```
outputs/bg_cleaned/<编号>_bg_cleaned.jpg
```

---

## 如果中途不满意怎么办？

首先在终端中使用快捷键Ctrl+C退出当前程序
然后重新运行 `python batch_process.py`，程序会问你从第几组开始，并询问是否覆盖已有结果。

---

完成以上步骤后，程序会自动进入下一组图片处理。
