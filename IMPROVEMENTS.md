# 改进记录

## 最近更新

- 新增 **批量处理**（按 4 位编号分组）
- 背景清理改为 **手动多次修复**
- 清理完成后支持 **可选竖线裁切**
- `main.py` 支持命令行参数，便于批处理

---

## 核心脚本

- `main.py` — 背景清理与合成交互
- `batch_process.py` — 批处理主入口
- `auto_clean.py` — 自动清理（可选）

---

## 输出目录

- `outputs/bg_cleaned/` — 清理后的背景
- `outputs/final/` — 最终合成图
