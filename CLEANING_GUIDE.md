# 背景清理指南

## 概述

本程序提供了两种背景清理方式：
1. **自动清理** - 自动检测并移除白色对象和金属尺子
2. **手动清理** - 交互式标记需要移除的区域

## 自动清理

### 原理

自动清理使用以下技术组合：

1. **HSV颜色空间分割** - 检测白色区域（低饱和度、高亮度）
2. **边缘检测 + 轮廓分析** - 识别细长的金属尺子
3. **形态学操作** - 优化掩码（闭操作填补小孔，开操作去除噪声）
4. **Telea算法** - 高质量图像修复（inpainting）

### 使用方法

```bash
# 基本自动清理
python auto_clean.py

# 手动验证模式（显示检测结果）
python auto_clean.py --manual
```

### 优点
- 快速自动处理
- 无需人工交互
- 适合批量处理

### 缺点
- 可能误检（例如背景中的自然阴影）
- 可能遗漏（例如与背景颜色相似的物体）

## 手动清理

在 `main.py` 中的交互式清理模式

### 使用步骤

1. 运行 `python main.py`
2. 当询问"是否现在清理背景？"时选择 `y`
3. 在显示的窗口中：
   - **左键拖拽** - 绘制矩形框选需要移除的区域
   - **可以标记多个区域** - 释放鼠标后可继续标记其他区域
   - **SPACE** - 确认标记并开始修复
   - **ESC** - 取消此次清理

### 优点
- 精确控制，不会误删
- 可以针对特定物体精确标记
- 支持复杂场景

### 缺点
- 需要用户交互
- 对于大量小物体可能耗时

## 修复算法详解

### Telea 算法

优点：
- 快速运算
- 边界保留良好
- 适合物体去除

缺点：
- 可能在大面积区域产生伪影

### Navier-Stokes 算法（备选）

优点：
- 物理基础更强
- 纹理合成效果好
- 大面积修复质量更高

缺点：
- 运算时间较长
- 对边界特征可能过度平滑

## 优化建议

### 如果修复结果不理想

1. **参数调整** - 在 `auto_clean.py` 中修改参数：
   ```python
   # 颜色范围调整（适应不同光照）
   white_lower = np.array([0, 0, 180])      # 降低亮度阈值
   white_upper = np.array([180, 100, 255])  # 提高饱和度容限
   
   # 修复radius（增加值可改善大面积伪影）
   result = cv2.inpaint(bg, mask, 15, cv2.INPAINT_TELEA)
   ```

2. **多步处理** - 先自动清理，再手动微调：
   ```bash
   python auto_clean.py         # 生成 bg_cleaned.png
   # 如果不满意，删除 bg_cleaned.png，再用手动模式
   python main.py               # 选择 y，使用手动清理
   ```

3. **使用不同算法** - 在 `auto_clean.py` 中切换：
   ```python
   # 替换此行：
   result = cv2.inpaint(bg, mask, 10, cv2.INPAINT_TELEA)
   # 改为：
   result = cv2.inpaint(bg, mask, 10, cv2.INPAINT_NS)
   ```

## 常见问题

### Q: 自动清理检测到的区域太多/太少？
A: 修改阈值参数：
```python
# 增加检测范围（检测更多灰色）
white_upper = np.array([180, 100, 255])  # 增加饱和度上限

# 增加尺寸过滤（忽略小物体）
if area > 1000:  # 增加最小面积
```

### Q: 修复后有明显伪影或接缝？
A: 
- 增加 radius 参数（从10增到15或20）
- 尝试 INPAINT_NS 算法
- 扩大掩码区域（形态学膨胀）

### Q: 手动清理时很难精确标记？
A: 
- 利用滚轮缩放窗口查看细节
- 多次小范围标记比一次大范围更精确
- 可以标记重叠的矩形，算法会合并处理

## 工作流程建议

### 首次使用新背景
```
1. python auto_clean.py          # 自动清理
2. python auto_clean.py --manual # 查看检测结果
3. python main.py                # 使用清理后的背景
```

### 如果自动结果不理想
```
1. rm images/bg_cleaned.png      # 删除自动结果
2. python main.py                # 选择 y 进入手动清理
3. 交互式标记需要移除的区域
```

### 批量处理多张背景图
```
# 编辑 auto_clean.py，添加批处理逻辑
# 或为每张背景创建单独的工作目录
```
