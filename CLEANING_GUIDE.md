# 背景清理指南

## 概述

本项目提供两种背景清理方式：
1. **自动清理**（`auto_clean.py`）
2. **手动清理**（`main.py` 内交互式清理）

当前自动清理策略已做简化：
- 白色牌子：**单个最大连通域** + **单轮 inpaint**
- 标尺：**从标尺右边开始裁切**，直接去掉左侧区域

## 自动清理

### 原理

1. **白色牌子检测（HSV）**
   - 通过 HSV 阈值检测“非常白”的区域
   - 连通域分析，只保留**最大块**作为白色牌子
   - 适度膨胀与形态学操作，提高掩码稳定性

2. **标尺检测（左侧、纵向）**
   - 只在左侧区域搜索
   - 挑出纵向“暗像素占比高”的细长区域
   - 只保留**一个标尺候选**

3. **处理逻辑**
   - 白色牌子：**单轮 Telea inpaint**
   - 标尺：**裁切掉标尺右边界以左区域**

### 使用方法

```bash
# 自动清理
python auto_clean.py

# 预览模式（仅用于检查检测区域）
python auto_clean.py --manual
```

### 优点
- 速度快（只做一轮 inpaint）
- 逻辑简单稳定

### 缺点
- 会改变图像宽度（标尺左侧被裁掉）

## 手动清理（main.py）

### 使用步骤

1. 运行 `python main.py`
2. 当询问“是否现在清理背景”时选择 `y`
3. 在弹窗中用鼠标框选需要移除的区域
4. 按 `SPACE` 确认，`ESC` 取消

### 优点
- 精确控制，不易误清
- 适合复杂场景

### 缺点
- 需要人工交互
- 大图会更耗时

## 常见问题

### Q: 自动清理裁剪太多/太少？
A: 调整 `auto_clean.py` 中的裁剪偏移（`crop_pad`）和标尺检测阈值。

### Q: 白色牌子没有清干净？
A: 可适当扩大白色牌子掩码的膨胀核或阈值范围。

## 工作流程建议

```
1. python auto_clean.py          # 自动清理 + 裁剪
2. python compare_bg.py          # 对比效果（可选）
3. python main.py                # 合成豆荚/种子
```
