# 背景清理指南

## 概述

本项目提供两种背景清理方式：
1. **手动清理**（main.py 内交互式清理，推荐）
2. **自动清理**（auto_clean.py，可选）

当前批处理流程采用 **手动清理**：
- 背景图先框选要移除的区域（inpaint 修复）
- 然后再框选豆荚和种子并放置

## 手动清理（推荐）

### 使用步骤

1. 运行 `python main.py` 或 `python batch_process.py`
2. 在背景窗口用鼠标框选需要移除的区域
3. 按 `SPACE` 确认，`ESC` 取消

### 优点
- 精确控制，不易误清
- 适合复杂场景

### 缺点
- 需要人工交互
- 大图会更耗时

## 自动清理（可选）

### 原理

1. 白色牌子检测（HSV + 连通域）
2. 标尺检测（左侧、纵向、暗像素占比）
3. 白牌单轮 inpaint
4. 标尺裁切（保留右侧）

### 使用方法

```bash
python auto_clean.py
```

## 常见问题

### Q: 批处理太慢怎么办？
A: 这是交互式流程，速度取决于人工操作时间。可以减少每组操作，或改为自动清理。

### Q: 只想自动清理背景？
A: 直接运行 `python auto_clean.py`。

## 工作流程建议

```
python batch_process.py    # 批量处理（全人工）
```
