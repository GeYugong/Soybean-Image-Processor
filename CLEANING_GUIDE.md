# 背景清理指南

本项目默认采用 **手动背景清理**，以保证结果稳定可控。

---

## 手动背景清理（推荐）

### 操作步骤
1) 弹出背景窗口后，用鼠标拖拽画矩形，覆盖需要移除的对象。
2) 按 **SPACE** 确认修复，按 **ESC** 取消。
3) 清理完成后窗口会再次弹出，可继续修复不满意的区域。
4) 清理结束后可选裁切：点击竖线保留右侧，按 **SPACE** 跳过。
5) 放置豆荚/种子前会提示选择“植株颜色参考”，用于色差校正。

### 提示
- 多画几个小矩形比一个大矩形更精准。
- 预览窗口按高度 900px 缩放，实际处理仍是原图尺寸。

---

## 自动清理（可选）

`auto_clean.py` 提供自动清理，但批处理默认使用手动模式。

```bash
python auto_clean.py
```

---

## 常见问题

- **清理后仍有残留**
  - 再次框选残留区域进行二次修复。

- **裁切过多或过少**
  - 重新处理该组，选择新的裁切线。

- **处理速度慢**
  - 手动流程主要耗时在交互操作。
